<?php
/**
 * module registration
 *
 * When you work on specific modules register them here.
 * And then set up particular routing rules accordingly if it's necessary.
 * This is an example. Copy file to "_module.php".
 *
 */

return array(
    'modules' => array(

    )
);